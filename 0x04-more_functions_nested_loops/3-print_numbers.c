#include "main.h"


/**
 * print_numbers - check for check for a digit (0 through 9).
 *
 * Retun: Always 0.
 */
void print_number(void)
{
	int c;

	for (c = 0; c < 10; c++)
	{
		putchar(c);
	}

	_putchar('\n');
}
